{{#with data}}

 <div class="card">
   <div class="id">
      
      <img src="{{photo}}" alt="card photo" class="card_image">
     
     
   </div>
        
            <h2 class="card_title">{{name}}</h2>
            <p class="card_text">{{description}}</p> 
        <button class="button card_button" href="{{href}}">Read All</button>
    </div>

{{/with}}